package com.bahthia.app.state

/** الشاشات المتاحة في التطبيق. */
enum class Screen {
    /** شاشة البَدء — تَظهر أَثناء تَحميل المكتبة (شعار + شَريط تَقدُّم). */
    SPLASH,

    /** شاشة الترحيب — تَفتح من قائمة "حول/الجولة" فقط (لم تَعد افتراضيّة). */
    WELCOME,

    /** الشاشة الرئيسية مع البحث والقارئ. */
    MAIN,

    /** شاشة "حول البرنامج". */
    ABOUT,
}
